apiVersion: apps/v1
kind: Deployment
metadata:
  name: hola-mundo-v2
spec:
  replicas: 1
  selector:
    matchLabels:
      app: hola-mundo
      version: v2
  template:
    metadata:
      labels:
        app: hola-mundo
        version: v2
    spec:
      containers:
      - name: hola-mundo
        image: nginx
        ports:
        - containerPort: 80
        volumeMounts:
        - name: html-volume
          mountPath: /usr/share/nginx/html
      volumes:
      - name: html-volume
        configMap:
          name: hola-mundo-html-v2
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: hola-mundo-html-v2
data:
  index.html: |
    <!DOCTYPE html>
    <html>
    <body>
    <h1>Hola Mundo v2</h1>
    </body>
    </html>