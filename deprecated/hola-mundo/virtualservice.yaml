apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: hola-mundo-vs
  namespace: hola-mundo
spec:
  hosts:
    - "hola.local"
  gateways:
    - hola-mundo-gateway
  http:
    - route:
        - destination:
            host: hola-mundo
            port:
              number: 80
